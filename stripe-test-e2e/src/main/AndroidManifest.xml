<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <meta-data
            android:name="com.stripe.android.test.e2e.metadata.backend_url"
            android:value="${BACKEND_URL}" />
        <meta-data
            android:name="com.stripe.android.test.e2e.metadata.publishable_key"
            android:value="${PUBLISHABLE_KEY}" />
    </application>
</manifest>
