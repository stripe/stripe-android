<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ExampleApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:supportsRtl="true"
        tools:ignore="AllowBackup">

        <meta-data
            android:name="com.stripe.android.paymentmethodmessaging.example.metadata.publishable_key"
            android:value="${PUBLISHABLE_KEY}" />
        <meta-data
            android:name="com.stripe.android.paymentmethodmessaging.example.metadata.stripe_account_id"
            android:value="${STRIPE_ACCOUNT_ID}" />

        <activity
            android:name="com.stripe.android.paymentmethodmessaging.example.MessagingElementActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
